# DevOps权限管理系统 - 环境变量配置示例
# 复制此文件为 .env 并填入实际值

# ==================== 数据库配置 ====================
# MySQL 数据库连接
DB_HOST=localhost
DB_PORT=3306
DB_NAME=devops2025
DB_USERNAME=your_db_username
DB_PASSWORD=your_db_password

# MySQL root 密码 (仅 Docker 部署时使用)
MYSQL_ROOT_PASSWORD=your_root_password

# ==================== JWT 配置 ====================
# JWT 密钥 (请使用强随机字符串，至少32字节)
# 可使用命令生成: openssl rand -base64 48
JWT_SECRET=your-very-strong-secret-key-at-least-32-characters-long
# Token 过期时间 (毫秒)，默认24小时
JWT_EXPIRATION=86400000

# ==================== LLM API 配置 ====================
# 支持 OpenAI 兼容接口（如 OpenAI、通义千问、本地 Ollama 等）

# API 地址
# OpenAI: https://api.openai.com/v1/chat/completions
# 通义千问: https://dashscope.aliyuncs.com/compatible-mode/v1/chat/completions
# Ollama: http://localhost:11434/v1/chat/completions
LLM_API_URL=https://api.openai.com/v1/chat/completions

# API Key
LLM_API_KEY=your-api-key-here

# 模型名称
# OpenAI: gpt-3.5-turbo, gpt-4
# 通义千问: qwen-turbo, qwen-plus
# Ollama: llama2, mistral
LLM_MODEL=gpt-3.5-turbo

# ==================== 应用配置 ====================
# 服务端口
SERVER_PORT=8080

# 日志级别: DEBUG, INFO, WARN, ERROR
LOG_LEVEL=INFO

# ==================== Docker 部署配置 ====================
# 后端服务端口映射
BACKEND_PORT=8080
# 前端服务端口映射
FRONTEND_PORT=80
# MySQL 端口映射
MYSQL_PORT=3306
